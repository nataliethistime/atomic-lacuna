# Just to make sure
rm -rf docs/code

grock --out "docs/code" --style thin \
    --glob "app/js/**/*.js"          \
    --glob "app/js/**/*.coffee"      \
    --glob "README.md"               \
    --glob "app/templates/**/*.hbs"  \
    --glob "app/css/*.css"

# TODO: replace all the emoji's with <img> things.
